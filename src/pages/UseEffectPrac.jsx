import { Outlet } from "react-router-dom"

export default function UseEffectPrac() {
    return (
        <div className="container">
            <h1>useEffectPrac</h1>
            <Outlet />
        </div>
    )
}